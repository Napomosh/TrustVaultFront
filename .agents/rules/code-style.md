---
trigger: always_on
---

1) class methods in CamelCase, private fields _camelCase, public fields camelCase, static fields CamelCase, const fields CAPS_LOCK, properties CamelCase
2) Use primary constructors and defines it to readonly private fields (or private field)
3) If if-block or else block has only one line curly brackets dont need. If and else block always strart from the new line. If if-blck or else-block or both has more than one line crule brackets need in all of this if-else branchs